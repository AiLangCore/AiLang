# AiLang.Core Native

Reserved location for C implementation of `AiLang.Core` during zero-C# migration.

Target:
- parser/tokenizer bridge
- AST/IR structures
- validator and deterministic language semantics
