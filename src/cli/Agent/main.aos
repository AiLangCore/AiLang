Program {
  Import(sdk="ailang" path="std/process.aos")
  Import(path="agent.aos")
  Export(name=main)

  Let(name=main) {
    Fn(params=args) {
      Block {
        Let(name=exitCode) { Call(target=runAgent) { Var(name=args) } }
        Call(target=exit) { Var(name=exitCode) }
        Return { Var(name=exitCode) }
      }
    }
  }
}
