[runtime]
# The self-hosted CLI payload is executed by AiVM. Keep this pinned to the
# release-tested runtime rather than inferring a potentially incompatible
# channel release at installation time.
aivmVersion = "0.0.1-rc.4"
