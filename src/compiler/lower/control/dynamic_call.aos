Program {
  Export(name=lower.dynamicCall.appendBinding)

  Let(name=lower.dynamicCall.appendBinding) {
    Fn(params=function,context,sequence,bindings,records,index,name,callNode) {
      Block {
        Let(name=candidate) { Call(target=lower.getAttr) { Var(name=callNode) Lit(value="candidates") Lit(value=0) } }
        If {
          Eq { Var(name=candidate) Lit(value="") }
          Block {
            Return {
              MakeErr {
                Lit(value="lower") Lit(value="LOWER034")
                Lit(value="CallDynamic requires one build-validated candidate.")
                NodeId { Var(name=callNode) }
              }
            }
          }
          Block {
            If {
              Eq { StringFind { Var(name=candidate) Lit(value=",") Lit(value=0) } Lit(value=-1) }
              Block {
                Return {
                  Call(target=lower.dynamicCall.appendSingleCandidateBinding) {
                    Var(name=function) Var(name=context) Var(name=sequence) Var(name=bindings)
                    Var(name=records) Var(name=index) Var(name=name) Var(name=callNode) Var(name=candidate)
                  }
                }
              }
              Block {
                Return {
                  MakeErr {
                    Lit(value="lower") Lit(value="LOWER034")
                    Lit(value="CallDynamic candidate sets are not yet supported by self-hosted lowering.")
                    NodeId { Var(name=callNode) }
                  }
                }
              }
            }
          }
        }
      }
    }
  }

  Let(name=lower.dynamicCall.appendSingleCandidateBinding) {
    Fn(params=function,context,sequence,bindings,records,index,name,callNode,candidate) {
      Block {
        Let(name=targetSlot) { Add { Call(target=lower.parameterCount) { Var(name=bindings) Lit(value=0) } Lit(value=1) } }
        Let(name=resultSlot) { Call(target=lower.parameterCount) { Var(name=bindings) Lit(value=0) } }
        Let(name=candidateLabel) { StrConcat { Lit(value="dynamic-call-candidate-") NodeId { Var(name=callNode) } } }
        Let(name=fallbackLabel) { StrConcat { Lit(value="dynamic-call-fallback-") NodeId { Var(name=callNode) } } }
        Let(name=mergeLabel) { StrConcat { Lit(value="dynamic-call-merge-") NodeId { Var(name=callNode) } } }
        Let(name=withTarget) {
          Call(target=lower.appendStructuralNativeExpression) {
            Var(name=context) ChildAt { Var(name=callNode) Lit(value=0) } Var(name=bindings) Var(name=records)
          }
        }
        If {
          Call(target=lower.isStructuralError) { Var(name=withTarget) }
          Block { Return { Var(name=withTarget) } }
          Block {
            Let(name=withTargetStore) {
              Call(target=blockBuilder.emit) {
                Var(name=withTarget) Lit(value="STORE_LOCAL") ToString { Var(name=targetSlot) }
              }
            }
            Let(name=withTargetLoad) {
              Call(target=blockBuilder.emit) {
                Var(name=withTargetStore) Lit(value="LOAD_LOCAL") ToString { Var(name=targetSlot) }
              }
            }
            Let(name=withCandidateName) {
              Call(target=blockBuilder.emit) {
                Var(name=withTargetLoad) Lit(value="CONST") StrConcat { Lit(value="string:") Var(name=candidate) }
              }
            }
            Let(name=withComparison) {
              Call(target=blockBuilder.emit) { Var(name=withCandidateName) Lit(value="EQ") Lit(value="") }
            }
            Let(name=withEntry) {
              Call(target=blockBuilder.appendBlock) {
                Var(name=function)
                Call(target=blockBuilder.seal) {
                  Call(target=blockBuilder.terminateBranch) {
                    Var(name=withComparison) Var(name=candidateLabel) Var(name=fallbackLabel)
                  }
                }
              }
            }
            Let(name=resolved) {
              Call(target=lower.resolveStructuralCallSymbol) { Var(name=records) Var(name=candidate) Lit(value=0) }
            }
            If {
              Eq { NodeKind { Var(name=resolved) } Lit(value="Err") }
              Block { Return { Var(name=resolved) } }
              Block {
                Let(name=candidateContext) {
                  Call(target=lower.dynamicCall.appendArguments) {
                    Call(target=blockBuilder.beginBlock) { Var(name=candidateLabel) }
                    Var(name=callNode) Var(name=bindings) Var(name=records) Lit(value=1)
                  }
                }
                If {
                  Call(target=lower.isStructuralError) { Var(name=candidateContext) }
                  Block { Return { Var(name=candidateContext) } }
                  Block {
                    Let(name=withCall) {
                      Call(target=blockBuilder.emit) {
                        Var(name=candidateContext) Lit(value="CALL") AttrValueString { Var(name=resolved) Lit(value=0) }
                      }
                    }
                    Let(name=withResultStore) {
                      Call(target=blockBuilder.emit) {
                        Var(name=withCall) Lit(value="STORE_LOCAL") ToString { Var(name=resultSlot) }
                      }
                    }
                    Let(name=withCandidateBlock) {
                      Call(target=blockBuilder.appendBlock) {
                        Var(name=withEntry)
                        Call(target=blockBuilder.seal) {
                          Call(target=blockBuilder.terminateJump) { Var(name=withResultStore) Var(name=mergeLabel) }
                        }
                      }
                    }
                    Let(name=fallbackContext) {
                      Call(target=blockBuilder.emit) {
                        Call(target=blockBuilder.emit) {
                          Call(target=blockBuilder.beginBlock) { Var(name=fallbackLabel) }
                          Lit(value="CONST") Lit(value="string:sys.dynamic.missingFunction")
                        }
                        Lit(value="LOAD_LOCAL") ToString { Var(name=targetSlot) }
                      }
                    }
                    Let(name=withFallbackCall) {
                      Call(target=blockBuilder.emit) { Var(name=fallbackContext) Lit(value="CALL_SYS") Lit(value="1") }
                    }
                    Let(name=withFallbackStore) {
                      Call(target=blockBuilder.emit) {
                        Var(name=withFallbackCall) Lit(value="STORE_LOCAL") ToString { Var(name=resultSlot) }
                      }
                    }
                    Let(name=withFallbackBlock) {
                      Call(target=blockBuilder.appendBlock) {
                        Var(name=withCandidateBlock)
                        Call(target=blockBuilder.seal) {
                          Call(target=blockBuilder.terminateJump) { Var(name=withFallbackStore) Var(name=mergeLabel) }
                        }
                      }
                    }
                    Return {
                      Call(target=lower.appendStructuralNativeTerminalSequence) {
                        Var(name=withFallbackBlock)
                        Call(target=blockBuilder.beginBlock) { Var(name=mergeLabel) }
                        Var(name=sequence)
                        Call(target=lower.appendStructuralNativeBinding) { Var(name=bindings) Var(name=name) }
                        Var(name=records) Add { Var(name=index) Lit(value=1) }
                      }
                    }
                  }
                }
              }
            }
          }
        }
      }
    }
  }

  Let(name=lower.dynamicCall.appendArguments) {
    Fn(params=context,callNode,bindings,records,index) {
      Block {
        If {
          Eq { Var(name=index) ChildCount { Var(name=callNode) } }
          Block { Return { Var(name=context) } }
          Block {
            Let(name=withArgument) {
              Call(target=lower.appendStructuralNativeExpression) {
                Var(name=context) ChildAt { Var(name=callNode) Var(name=index) }
                Var(name=bindings) Var(name=records)
              }
            }
            If {
              Call(target=lower.isStructuralError) { Var(name=withArgument) }
              Block { Return { Var(name=withArgument) } }
              Block {
                Return {
                  Call(target=lower.dynamicCall.appendArguments) {
                    Var(name=withArgument) Var(name=callNode) Var(name=bindings)
                    Var(name=records) Add { Var(name=index) Lit(value=1) }
                  }
                }
              }
            }
          }
        }
      }
    }
  }
}
